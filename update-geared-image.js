require('dotenv').config({path: '.env.local'}); const mongoose = require('mongoose'); mongoose.connect(process.env.MONGODB_URI).then(async () => { const Category = require('./src/models/Category').default; await Category.findOneAndUpdate({ slug: 'geared-8852' }, { 'image.url': '/pictures/geared-category.png' }); console.log('Updated Geared category image URL to .png'); process.exit(0); })
