require('dotenv').config({path: '.env.local'}); const mongoose = require('mongoose'); mongoose.connect(process.env.MONGODB_URI).then(async () => { const Category = require('./src/models/Category').default; const Product = require('./src/models/Product').default; const geared = await Category.findOne({slug: 'geared'}); const products = await Product.find({category: geared._id}); console.log('Geared products count:', products.length); process.exit(0); })
